---
export interface Props {
  steps: Array<{
    number: string;
    title: string;
    description: string;
    duration: string;
  }>;
}

const { steps } = Astro.props;
---

<section id="proceso" class="py-20 bg-gradient-to-b from-dark-950 to-dark-900">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Section Header -->
    <div class="text-center mb-16 animate-slide-up">
      <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">
        Nuestro proceso de
        <span class="bg-gradient-to-r from-primary-400 to-secondary-400 bg-clip-text text-transparent">
          trabajo
        </span>
      </h2>
      <p class="text-xl text-gray-300 max-w-3xl mx-auto leading-relaxed">
        Un método probado que garantiza resultados excepcionales en cada proyecto
      </p>
    </div>

    <!-- Process Steps -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {steps.map((step, index) => (
        <div 
          class="relative group animate-fade-in"
          style={`animation-delay: ${index * 0.2}s`}
        >
          {/* <!-- Step Card --> */}
          <div class="bg-dark-900/50 backdrop-blur-sm border border-gray-800 rounded-2xl p-8 text-center hover:bg-dark-800/50 hover:border-primary-500/50 transition-all duration-300 hover:scale-105 hover:shadow-xl hover:shadow-primary-500/10">
            
            {/* <!-- Step Number --> */}
            <div class="relative mb-6">
              <div class="w-20 h-20 bg-gradient-to-r from-primary-500 to-secondary-500 rounded-full flex items-center justify-center text-2xl font-bold text-white mx-auto group-hover:scale-110 transition-transform duration-300">
                {step.number}
              </div>
              {index < steps.length - 1 && (
                <div class="hidden lg:block absolute top-10 -right-12 w-24 h-0.5 bg-gradient-to-r from-primary-500/50 to-transparent"></div>
              )}
            </div>
            
            {/* <!-- Content --> */}
            <h3 class="text-xl font-bold text-white mb-4 group-hover:text-primary-100 transition-colors duration-200">
              {step.title}
            </h3>
            
            <p class="text-gray-300 leading-relaxed mb-4">
              {step.description}
            </p>
            
            {/* <!-- Duration --> */}
            <div class="inline-flex items-center px-3 py-1 bg-primary-500/20 text-primary-300 rounded-full text-sm font-medium border border-primary-500/30">
              <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              {step.duration}
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Bottom CTA -->
    <div class="text-center mt-16">
      <p class="text-gray-300 mb-6">
        ¿Tienes dudas sobre nuestro proceso? Hablemos
      </p>
      <a 
        href="/contacto"
        class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-secondary-500 to-secondary-600 text-white font-semibold rounded-full hover:shadow-lg hover:shadow-secondary-500/25 transition-all duration-200 hover:scale-105"
      >
        Agendar consulta gratuita
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
        </svg>
      </a>
    </div>
  </div>
</section>